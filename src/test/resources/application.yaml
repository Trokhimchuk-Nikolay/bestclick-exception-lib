spring:
  messages:
    encoding: UTF-8
    basename: messages/api_error_test
  main:
    allow-bean-definition-overriding: true
  jackson:
    deserialization:
      read-date-timestamps-as-nanoseconds: false
    serialization:
      write-dates-as-timestamps: false

bestclick:
  exception:
    enable: true